pip>=24.3.1
sentencepiece==0.1.99
transformers==4.35.0
torch>=2.0.1; platform_system != "Windows"  # Adjusts for PyTorch compatibility
torch>=2.0.0+cpu; platform_system == "Windows"  # CPU-only for Windows
streamlit==1.25.0
